interface:
  display_name: "Safe Remote Support"
  short_description: "Run policy-bound remote development sessions"
  default_prompt: "Use $safe-remote-support to create a visible remote development session, run scoped tasks, review evidence, and close access on operator direction."
policy:
  allow_implicit_invocation: true
