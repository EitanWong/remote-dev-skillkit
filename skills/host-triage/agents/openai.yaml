interface:
  display_name: "Host Triage"
  short_description: "Probe host readiness safely"
  default_prompt: "Use $host-triage to inspect the target host and return a readiness report before any remote work."
policy:
  allow_implicit_invocation: true
